package com.epam.project.rest;

/**
 * Created by StepLuch on 08.09.15.
 */

public class LoginActionTest {
 /*   @Override
    protected Application configure() {
        return new ResourceConfig().register(LoginAction.class);
    }

    @Test
    public void test() throws JSONException {
        *//*Form form = new Form();
        form.param("username", "StepanLu");
        form.param("password", "5197692");
        Response response = target("/login").queryParam("username", "StepanLu").queryParam("password", "5197692").request().post(Entity.text(new JSONObject().put("username", "StepanLu").toString()));
        assertEquals(Response.Status.OK.getStatusCode(), response.getStatus());*//*
    }
*/
}
